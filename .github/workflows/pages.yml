name: Build and Deploy GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate site
        env:
          APP_NAME: DailyScope
          BASE_URL: https://dailyscopepost.com
          LANGUAGE: ${{ secrets.LANGUAGE }}
          COUNTRY: ${{ secrets.COUNTRY }}
          GNEWS_API_KEY: ${{ secrets.GNEWS_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python generate.py

      - name: Copy article images
        run: |
          mkdir -p dist/static/images
          cp -r app/content/images/. dist/static/images/

      - name: DEBUG final dist
        run: |
          echo "=== IMAGES ==="
          find dist/static/images -maxdepth 1 -type f | head -20
      
          echo "=== ARTICLE HTML ==="
          grep -R 'static/images\|content/images' dist/article | head -20
      
          echo "=== INDEX HTML ==="
          grep -R 'static/images\|content/images' dist/index.html | head -20

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build

    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
